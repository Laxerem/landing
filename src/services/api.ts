import type { SiteContent } from '../types/content';

// Phase 2: replace with real HTTP call to the backend API
export async function fetchContent(): Promise<SiteContent> {
  throw new Error('API not implemented yet — use static data');
}
